@model List<Hipodromo_Nacional.ViewModels.CarreraListaViewModel>
@{
    ViewData["Title"] = "Control de Inscripciones";
}

<div class="card-hipodromo">
    <div class="card-header-custom">
        <h5><i class="fas fa-list-check me-2"></i>Control de Inscripción a Carreras</h5>
        <a href="@Url.Action("Index")" class="btn-outline-gold">
            <i class="fas fa-flag-checkered me-1"></i> Carreras
        </a>
    </div>
    <div class="card-body-custom p-0">
        @if (!Model.Any())
        {
            <div class="text-center py-5" style="color:var(--text-muted)">
                <i class="fas fa-list-check" style="font-size:3rem;opacity:.3"></i>
                <p class="mt-3">No hay carreras disponibles para gestionar inscripciones.</p>
            </div>
        }
        else
        {
            <div style="overflow-x:auto">
                <table class="table-hipodromo">
                    <thead>
                        <tr>
                            <th>Código</th>
                            <th>Carrera</th>
                            <th>Fecha</th>
                            <th>Tipo</th>
                            <th>Estado</th>
                            <th>Caballos inscritos</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var c in Model)
                        {
                            var estado = c.Estado?.ToLower() ?? "";
                            var estadoClass = estado.Contains("cancel") || estado.Contains("suspend")
                                ? "badge-danger"
                                : estado.Contains("final") || estado.Contains("cerr")
                                    ? "badge-info"
                                    : estado.Contains("program") || estado.Contains("abiert")
                                        ? "badge-ok"
                                        : "badge-warn";

                            <tr>
                                <td><span style="color:var(--gold);font-weight:600">@c.CodigoEvento</span></td>
                                <td style="font-weight:500">@c.Nombre</td>
                                <td>@c.FechaEvento.ToString("dd/MM/yyyy HH:mm")</td>
                                <td>@c.TipoCarrera</td>
                                <td><span class="badge-salud @estadoClass">@c.Estado</span></td>
                                <td>@c.TotalInscritos</td>
                                <td>
                                    <a href="@Url.Action("Inscripciones", new { id = c.IdEvento })" class="btn-gold">
                                        <i class="fas fa-list-check me-1"></i> Gestionar
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
